{
  "name": "the-all-dash",
  "private": true,
  "version": "0.2.0-rc.6",
  "type": "module",
  "description": "A command center for a project or a person: ingest documents, calendars and spreadsheets, and get one live dashboard out of them.",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "tauri": "tauri",
    "release": "node scripts/release.mjs",
    "sample-workspace": "node scripts/sample-workspace.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.2.2"
  }
}
