{
  "name": "the-all-dash",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A command center for a project or a person: ingest documents, calendars and spreadsheets, and get one live dashboard out of them.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
