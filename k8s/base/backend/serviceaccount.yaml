# Shared by the API, worker, beat and frontend pods. It has no RBAC rules
# and its token is not mounted; it exists so IRSA can be attached later
# (for example, for S3 exports) without touching every Deployment.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: alldash-app
  namespace: alldash
automountServiceAccountToken: false
