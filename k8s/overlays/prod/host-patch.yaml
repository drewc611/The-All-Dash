# The rule host. The TLS host (Let's Encrypt path) and the public host used
# elsewhere come from PUBLIC_HOST in config-patch.yaml through `replacements`.
- op: replace
  path: /spec/rules/0/host
  value: alldash.example.com
