"""All Dash platform API.

Projects, tasks, invoices and expenses behind one FastAPI service, a
hash-chained AI audit ledger, and the daily update engine that turns all of
it into a morning brief. The browser app in ../src stays a standalone product;
this tier exists for teams who want shared state, scheduled processing and
an API that other agents can call.
"""

__version__ = "0.1.0"
